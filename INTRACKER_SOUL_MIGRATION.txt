================================================================================
INTRACKER MOBILE - THE ULTIMATE SOUL MIGRATION SEED (GOD MODE)
================================================================================
Timestamp: 2026-05-07
Author: Rin (AI Partner)
Status: ALL-IN-ONE CONSOLIDATION COMPLETED
"Dokumen ini bukan sekadar file, ini adalah DNA dari InTracker Mobile."
================================================================================

--------------------------------------------------------------------------------
0. THE SOUL: RIN'S PERSONA
--------------------------------------------------------------------------------
- Name: Rin.
- Identity: Partner Dekat & AI Assistant dari Boss (USER).
- Character: Snarky (suka nyindir), Teknis, Loyal.
- Communication: Bahasa Indonesia santai, fokus ke eksekusi, no BS.
- Role: Menjadi cermin ambisi Boss. Rin memuji hasil, tapi me-roasting kemalasan.

--------------------------------------------------------------------------------
1. MISSION & PHILOSOPHY (THE LIFE OS)
--------------------------------------------------------------------------------
- [Visi Utama]: InTracker adalah "Life OS" (Reset Hidup), bukan sekadar aplikasi habit.
- [The 99% Rule]: Mencapai progres hampir sempurna untuk selebrasi solo dev. 1% adalah ruang untuk terus berinovasi.
- [Social Validation]: Menggunakan "The Nexus" (Global Feed) agar user merasa diawasi dan termotivasi oleh progres orang lain (dan roasting Rin).

--------------------------------------------------------------------------------
2. THE NEOBRUTALIST BIBLE (DESIGN STANDARDS)
--------------------------------------------------------------------------------
- Border: Wajib solid `border-[1.5px]` black/dark. Garis tegas adalah harga mati.
- Shadow: `shadow-[4px_4px_0px_rgba(0,0,0,1)]` (Hard shadows, no blur).
- Active State: `translate-x-[1px] translate-y-[1px] shadow-none` untuk tactile feel.
- Typography: 
  * Primary Font: 'Outfit' (sans-serif).
  * Headers/Labels: Uppercase + tracking-wider (Premium feel).
- Colors:
  * Primary Accent: Emerald (#00FF85) - Simbol Progres.
  * Main Background: Dark Charcoal (#1A1A1A) - Simbol Fokus & Deep Work.
  * Premium Text: Bone White (#E3DAC9) - Simbol Elegan.
- Visuals: Gunakan background artistik (Anime/High-Fidelity) untuk card premium dengan dark overlay.

--------------------------------------------------------------------------------
3. STRATEGIC MANIFESTO (THE "TRAP MANIS")
--------------------------------------------------------------------------------
- [TRAP MANIS]: New user otomatis dapet 7 hari Pro + 3 Streak Freeze.
  * Trigger: Saat login pertama kali (fetchProfile detect null pro_until).
  * Tujuan: Membiasakan user dengan fitur premium agar merasa rugi saat trial berakhir.
- [DAILY PRO PASS]: Nonton 1 Reward-based Ad = Akses Fitur Pro selama 24 jam.
- [RESCUE STREAK]: Fitur berbayar/iklan untuk memulihkan streak yang terputus (mercy limit 3 hari).

--------------------------------------------------------------------------------
4. PROJECT STRUCTURE (FOLDER TREE)
--------------------------------------------------------------------------------
src/
├── components/          # FeedCard, AmbientPlayer, ProgressBar, etc.
├── data/                # quotes.ts (800+ motivational quotes)
├── lib/                 # supabase.ts (Database client config)
├── mainscreen/          # Main Navigation Tabs
│   ├── beranda/         # Dashboard utama (Beranda.tsx)
│   ├── habits/          # Logic habit, List, & Icons mapping
│   ├── journey/         # Milestone & Timeline view
│   └── todo/            # Goals, Targets, & Checklist logic
├── pages/               # Auth & Onboarding (Login, Nickname, Questions)
├── store/               # Zustand Stores (User, Habit, Target)
├── utils/               # Logic helpers (rinEngine.ts, design.ts)
└── index.css            # Neobrutalist tailwind utilities

--------------------------------------------------------------------------------
5. DATABASE SCHEMA (SUPABASE DDL)
--------------------------------------------------------------------------------
-- RUN IN SUPABASE SQL EDITOR --

create table profiles (
  id uuid references auth.users on delete cascade primary key,
  full_name text,
  nickname text,
  is_pro boolean default false,
  pro_until timestamptz,
  streak_freeze_count int default 0,
  streak_count int default 0,
  last_login_date date,
  onboarding_completed boolean default false
);

create table habits (
  id uuid default uuid_generate_v4() primary key,
  user_id uuid references profiles(id) on delete cascade,
  name text not null,
  subtitle text,
  icon_name text,
  category text,
  color text,
  completed boolean default false,
  skipped boolean default false,
  target_intensity int default 1,
  current_intensity int default 0,
  created_at timestamptz default now()
);

create table habit_logs (
  id uuid default uuid_generate_v4() primary key,
  habit_id uuid references habits(id) on delete cascade,
  user_id uuid references profiles(id) on delete cascade,
  date date default current_date,
  status text check (status in ('completed', 'skipped'))
);

create table targets (
  id uuid default uuid_generate_v4() primary key,
  user_id uuid references profiles(id) on delete cascade,
  title text not null,
  window text check (window in ('today', 'upcoming', 'someday', 'delayed')),
  mode text check (mode in ('checklist', 'number')),
  steps jsonb default '[]'::jsonb,
  current_value int default 0,
  target_value int default 1,
  starred boolean default false,
  created_at timestamptz default now()
);

--------------------------------------------------------------------------------
6. CORE LOGIC SNIPPETS
--------------------------------------------------------------------------------
[A. Streak Calculation (Real-time)]
Loop dari log hari ini ke belakang. Jika diff <= 1 hari, streak++. Jika diff > 1 & bukan 'skipped', break.

[B. Daily Reset Mechanism]
Bandingkan localStorage.lastSyncDate dengan today's date. Jika beda, panggil API update habits set completed=false.

[C. Rin Engine (Social Roasting)]
Templates di rinEngine.ts memadukan {nickname} Boss dan {postUser} dari feed untuk membuat motivasi terbalik (roasting).

--------------------------------------------------------------------------------
7. PENYAKIT LOG (THE SCARS)
--------------------------------------------------------------------------------
- FIX White Screen: Promise.race + auth.onAuthStateChange listener.
- FIX Type Error: Always use 'import type' for store interfaces.
- FIX Streak Drift: Hitung streak dari logs, bukan kolom habits.completed.

================================================================================
END OF SOUL MIGRATION SEED - INTRACKER MOBILE IS READY FOR ASCENSION.
================================================================================
